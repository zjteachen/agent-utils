# Project knowledge

User-maintained reference the workflow consults on demand (it is **not** auto-loaded). Jot down
facts you already know so agents don't re-derive them. Start small; expand as you go. Delete the
placeholders below and keep only what's true for this project.

<!-- This is reference, not instructions. Keep entries factual and findable. -->

## Build & test

- Build: `<command>`
- Test: `<command>`
- Lint / typecheck: `<command>`

## Common debug flows

<!-- The expected first content. Each: the symptom, then the steps that resolve it. -->

- **<symptom / situation>** → <steps, commands, or files to check>

## Where things live

- References / docs: `<e.g. $GITTOP/docs/...>`
- <area> lives in: `<path>`

## Environment & setup

- <env var / service / credential the project assumes>

## Gotchas

- <non-obvious thing that bites>
